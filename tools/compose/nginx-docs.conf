server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Docusaurus generates static HTML — try exact file, then directory, then .html extension
    location / {
        try_files $uri $uri/ $uri.html =404;
    }
}
